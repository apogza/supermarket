<form action="{{ url('delete_discount', {id: discount.id}) }}" method="post" onsubmit="return confirm('Are you sure you want to delete?')" id="delete-form">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit">Delete</button>
</form>
